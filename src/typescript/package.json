{
  "name": "@airlock/gateway-sdk",
  "version": "0.1.0",
  "description": "Client SDK for the Airlock Gateway — submit artifacts, poll for decisions, manage pairing and presence.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "keywords": ["airlock", "gateway", "sdk", "harp", "approval"],
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
